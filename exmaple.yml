---
- name: Hello World!
  hosts: all
 
  tasks:
 
  - name: Hello World!
    shell: echo "Hi! Tower is working."

  - name: Execute single "show interfaces" command.
    junos_command:
      commands: "show interfaces"
      register: response
